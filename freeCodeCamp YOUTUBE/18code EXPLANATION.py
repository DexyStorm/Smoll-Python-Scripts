b = 1
while b <=10:
    print(b)
    b = b + 1

print ("Done with Loop")

# Everything that is indented, Python will LOOP that code.
# (Looping means start the code from new, but only the indented code.)